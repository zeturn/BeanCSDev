apiVersion: v1
kind: Secret
metadata:
  name: beancs-postgres
  namespace: beancs-system
type: Opaque
stringData:
  POSTGRES_DB: beancs
  POSTGRES_USER: beancs
  POSTGRES_PASSWORD: change-me-before-production
---
apiVersion: v1
kind: Service
metadata:
  name: beancs-postgres
  namespace: beancs-system
  labels:
    app: beancs-postgres
    managed-by: beancs
spec:
  type: ClusterIP
  selector:
    app: beancs-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: beancs-postgres
  namespace: beancs-system
  labels:
    app: beancs-postgres
    managed-by: beancs
spec:
  serviceName: beancs-postgres
  replicas: 1
  selector:
    matchLabels:
      app: beancs-postgres
  template:
    metadata:
      labels:
        app: beancs-postgres
        managed-by: beancs
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - name: postgres
              containerPort: 5432
          envFrom:
            - secretRef:
                name: beancs-postgres
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - beancs
                - -d
                - beancs
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - beancs
                - -d
                - beancs
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 10Gi
